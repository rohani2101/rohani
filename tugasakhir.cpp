#include <iostream>
using namespace std;
int main()
{
    int kendaraan, masuk, keluar, lama, biaya;
    cout << "====== PARKIR TOKO =========" << endl;
    cout << " " << endl;

    cout << "jenis kendaraan (1. motor || 2.mobil) :";
    cin >> kendaraan;
    cout << "jam_masuk :";
    cin >> masuk;
    cout << " jam_keluar:";
    cin >> keluar;

    cout << "=========  BIAYA PARKIR ===========" << endl;
    /* lama = keluar, masuk;
     if (kendaraan == 1)
     {
         cout << "jenis kendaraan"
              << ":"
              << "motor" << endl;
     }
     else if (kendaraan == 2)
     {
         cout << "jenis kendaraan :"
              << "mobil" << endl;
     }
     else
     {
         cout << "maaf anda salah memasukan jenis kendaraan" << endl;
     }

     // biaya parkir motor
     if (lama <= 5 && kendaraan == 1)
     {
         biaya = (lama - 1) * 1000 + 1000;
     }
     else if (lama >= 4 && kendaraan == 1)
     {
         biaya = 15000;
     }

     // biaya parkirmobil
     else if (lama <= 5 && kendaraan == 2)
     {
         biaya = (lama - 1) * 3000 + 300;
     }
     else if (lama >= 4 && kendaraan == 2)
     {
         biaya = 15000;
     }
     // else
     //{
     //    cout << "maaf anda salah memasukan jenis kendaraan" << endl;
     // }
     cout << "lama parkir :" << lama << "jam" << endl;
     cout << "biaya parkir :"
          << "Rp." << biaya << endl;
     cout << "=========== TRIMAKASSIH ========" << endl;
     return 0;
     */
}
